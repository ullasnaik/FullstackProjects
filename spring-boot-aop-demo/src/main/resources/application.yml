server:
  port: 8081
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/allstate_demo
    username: root
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
logging:
  level:
    root: INFO