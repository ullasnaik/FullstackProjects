class Node {
    constructor(element) {
        this.element = element;
        this.next = null
    }
}

class LinkedList {
    constructor() {
        this.head = null;
        this.size = 0;
    }

    // addItem function to add new item in LinkedList
    addItem(element) {
        
    }

    // removeItem function to remove an item based on index in LinkedList
    removeItemAt(index) {
        
    }

    // getAllItems function to get all items from LinkedList
    getAllItems() {
        
    }

    // getItemAt function to get an item from LinkedList based on index
    getItemAt(index) {
        
    }

    // indeOf function to get the index of an item
    indexOf(element) {
        
    }
}

module.exports = LinkedList;